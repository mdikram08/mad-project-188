<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smarthostel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHostel">
        
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".RegisterActivity" />
        <activity android:name=".StudentDashboardActivity" />
        <activity android:name=".WardenDashboardActivity" />

        <activity android:name=".LeaveRequestActivity" />
        <activity android:name=".ComplaintActivity" />
        <activity android:name=".FoodReviewActivity" />
        <activity android:name=".RoomRequestActivity" />

        <activity android:name=".ManageLeaveActivity" />
        <activity android:name=".ManageComplaintActivity" />
        <activity android:name=".ManageRoomActivity" />
        <activity android:name=".ViewFoodReviewsActivity" />

    </application>

</manifest>
